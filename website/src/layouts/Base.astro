---
import "../styles/global.css";
import { ClientRouter } from "astro:transitions";

export interface Props {
  title?: string;
  description?: string;
  path?: string;
}

const {
  title = "ClamUI - Antivirus, native to your Linux desktop",
  description = "A modern GTK4 / libadwaita front-end for ClamAV. Scan profiles, quarantine with SHA-256 verification, a system security audit, VirusTotal enrichment - open-source and MIT-licensed.",
  path = "/",
} = Astro.props;

const siteUrl = "https://clamui.com";
const canonical = new URL(path, siteUrl).toString();
const ogImage = new URL("/og-image.png", siteUrl).toString();
---

<!doctype html>
<html lang="en" class="dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="color-scheme" content="dark light" />
    <meta name="theme-color" content="#0f1721" media="(prefers-color-scheme: dark)" />
    <meta name="theme-color" content="#fbfcfa" media="(prefers-color-scheme: light)" />

    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="mask-icon" href="/favicon.svg" color="#3d7a4e" />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="ClamUI" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:type" content="image/png" />
    <meta property="og:image:width" content="1280" />
    <meta property="og:image:height" content="640" />
    <meta property="og:image:alt" content="ClamUI branded preview with scan results and security features" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />
    <meta name="twitter:image:alt" content="ClamUI branded preview with scan results and security features" />

    <script is:inline>
      // Theme bootstrap: honor saved preference, then OS, then default to dark.
      (function () {
        try {
          const stored = localStorage.getItem("clamui-theme");
          const prefersLight =
            stored === "light" ||
            (!stored &&
              window.matchMedia("(prefers-color-scheme: light)").matches);
          document.documentElement.classList.toggle("light", prefersLight);
          document.documentElement.classList.toggle("dark", !prefersLight);
        } catch (_) {}
      })();
    </script>

    <ClientRouter />
  </head>
  <body class="min-h-screen antialiased">
    <a
      href="#main"
      class="sr-only focus:not-sr-only focus:fixed focus:top-3 focus:left-3 focus:z-50 focus:bg-clam-forest focus:text-white focus:px-3 focus:py-2 focus:rounded-md"
    >
      Skip to content
    </a>
    <slot />
  </body>
</html>
